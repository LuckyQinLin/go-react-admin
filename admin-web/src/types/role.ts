namespace Role {
    // 用户角色信息
    export interface UserRoleProp {
        roleId: number;    // 角色ID
        roleName: string;  // 角色名称
        roleCode: string;  // 角色编码
    }
}

export default Role;
